import styled from "@emotion/styled";

const StyledListActivity = styled.div`
  .ant-col {
    margin-top: 4px;
  }

  .action-buttons {
    display: flex;
    justify-content: end;
  }
`;

export default StyledListActivity;
