import styled from "@emotion/styled";
import { Card } from "antd";

const StyledWaterCounter = styled(Card)`
  max-width: 400px;
  min-width: 376px;

  .ant-radio-button-wrapper {
    border-radius: 0;
  }
`;

export default StyledWaterCounter;
